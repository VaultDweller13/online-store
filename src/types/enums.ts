export enum Elements {
  input = 'INPUT',
  label = 'LABEL',
  checkbox = 'checkbox',
  textInput = 'text',
  range = 'range',
}

export enum Colors {
  sliderRange = 'blue',
  sliderDefault = 'gray',
}
