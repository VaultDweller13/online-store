export enum Elements {
  input = 'INPUT',
  label = 'LABEL',
  checkbox = 'checkbox',
  textInput = 'text',
  range = 'range',
}

export enum Colors {
  sliderRange = '#244d4d',
  sliderDefault = '#e3e3e3',
}
